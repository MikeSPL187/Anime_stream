import 'package:flutter/material.dart';

abstract final class AppThemeTokens {
  static const background = Color(0xFF090909);
  static const surface = Color(0xFF111111);
  static const surfaceElevated = Color(0xFF181818);
  static const surfaceMuted = Color(0xFF1F1F1F);
  static const outline = Color(0xFF2A2A2A);
  static const outlineStrong = Color(0xFF393939);

  static const primary = Color(0xFFFF6B00);
  static const primaryPressed = Color(0xFFE65F00);
  static const primaryContainer = Color(0xFF3A1F0A);

  static const onBackground = Colors.white;
  static const onSurface = Colors.white;
  static const onSurfaceMuted = Color(0xFFB8B8B8);
  static const onSurfaceFaint = Color(0xFF8D8D8D);

  static const pagePadding = 20.0;
  static const cardRadius = 20.0;
  static const controlRadius = 16.0;
  static const chipRadius = 999.0;
  static const railGap = 12.0;

  static const pageTopGap = 20.0;
  static const sectionGap = 28.0;
}