import 'package:flutter/material.dart';

abstract final class AppThemeTokens {
  static const background = Color(0xFF060606);
  static const surface = Color(0xFF0D0D0D);
  static const surfaceElevated = Color(0xFF121212);
  static const surfaceMuted = Color(0xFF161616);

  static const outline = Color(0x14FFFFFF);
  static const outlineStrong = Color(0x22FFFFFF);

  static const primary = Color(0xFFFF7A00);
  static const primaryPressed = Color(0xFFE56D00);
  static const primaryContainer = Color(0xFF2A1709);

  static const onBackground = Colors.white;
  static const onSurface = Colors.white;
  static const onSurfaceMuted = Color(0xFFC4C4C4);
  static const onSurfaceFaint = Color(0xFF979797);

  static const pagePadding = 16.0;
  static const cardRadius = 18.0;
  static const controlRadius = 16.0;
  static const chipRadius = 999.0;
  static const railGap = 10.0;

  static const pageTopGap = 12.0;
  static const sectionGap = 24.0;
}
